# VoltPulse 技术决策记录 (DECISIONS)

本文档记录 VoltPulse 系统架构、算法建模、数据口径与工程实现的关键技术决策及其背后的权衡与理由，供工程追溯和面试答辩引用。

---

## 1. 市场与数据口径选择

- **选定市场**：中国山东电力现货市场（Shandong Power Spot Market）。
- **价格口径**：日前统一出清价格（Day-Ahead Unified Clearing Price, DA）。
- **时间粒度**：15 分钟（单日 96 时段，`interval_minutes=15`）。
- **时区口径**：`Asia/Shanghai` (UTC+8)。时段定义为 `[interval_start, interval_end)` 前闭后开区间。
- **决策理由**：
  - 山东是中国现货试点中最早建立成熟负电价出清机制的省份，受大规模分布式光伏接入影响，“鸭子曲线”特征极强，正负价差极大，是验证储能套利算法与系统调节价值的最佳样本。
  - 相比实时价格（RT）存在部分缺失与临时调频修正，日前价格数据格式规范、披露规律稳定、权威可追溯。

---

## 2. 储能调度优化：HiGHS MILP 与充放电互斥二元变量

- **建模选择**：混合整数线性规划（Mixed-Integer Linear Programming, MILP），引入二元状态变量 $u_t \in \{0, 1\}$。
- **约束方程**：
  $$0 \le c_t \le P_c u_t$$
  $$0 \le d_t \le P_d (1 - u_t)$$
- **求解器**：SciPy 1.17+ 内置的 HiGHS MIP Solver（通过 `scipy.optimize.milp`）。
- **替代方案与否决理由**：
  - *替代方案 A（纯 LP + 效率损失自然互斥）*：在正常正电价下，由于往返效率 $\eta < 100\%$，纯 LP 求解器通常不会同时充放电。但在负电价下（例如 $p_t = -100$ 元/MWh），如果边际衰减成本较低，纯 LP 求解器会发现同时满功率充电（收取消纳补贴）并满功率放电（通过损耗直接烧掉电能）具有数学套利空间，导致物理失真。
  - *替代方案 B（惩罚因子法 / 规则切除法）*：人为在目标函数加入惩罚因子或硬性把负价期间放电上限设为 0。该方法破坏了模型的通用数学严密性，无法推广到含网损与复杂电价环境。
  - *最终决策*：V2 规范第 10.2 条强制要求使用 MILP 显式二元变量 $u_t$ 物理互斥，杜绝任何价格场景下的同时充放电行为。

---

## 3. 电芯侧能量状态变量与双向吞吐量 $Q$ 统一口径

- **状态变量**：$E_t$ 代表电池电芯侧（DC side）储存能量（MWh）。
- **能量演化方程**：
  $$E_{t+1} = E_t + \eta_c c_t \Delta t - \frac{d_t \Delta t}{\eta_d}$$
  其中 $\eta_c = \eta_d = \sqrt{0.85} \approx 0.92195$。
- **电芯侧双向吞吐量 $Q$**：
  $$Q = \sum_t \left( \eta_c c_t + \frac{d_t}{\eta_d} \right) \Delta t$$
- **折旧成本计算**：$k_{deg} Q$，基准假设 $k_{deg} = 30$ 元/MWh。
- **等效循环次数（EFC）双口径**：
  - 额定容量 EFC：$EFC_{rated} = \frac{Q}{2 E_{nom}}$
  - 可用窗口 EFC：$EFC_{usable} = \frac{Q}{2 (E_{max} - E_{min})}$
- **决策理由**：
  - 许多简化模型仅按交流侧放电量计算折旧或 EFC，忽略了充电侧能量输入对电池老化的贡献，低估了 15% 左右的老化成本。
  - 采用电芯侧能量通量，将充放电不对称效率精确折算至电芯实际承受的化学能通量，学术严谨度与工程真实度完全一致。

---

## 4. 固定时段规则基准策略（Fixed Peak-Valley）终端自洽设计

- **充放策略**：依据山东省典型光伏午间低谷（11:00-15:00 充电）与晚间负荷高峰（18:00-22:00 放电）预先配置时段规则，严禁读取当日价格偷看最优窗口。
- **终端 SOC 约束**：充电时段由初始 $E_0 = 100$ MWh 充至上限 $E_{max} = 180$ MWh；放电时段放至目标终态 $E_{final} = 100$ MWh 后立即进入待机闲置状态。
- **决策理由**：
  - 若放电阶段直接放空至 $E_{min} = 20$ MWh，则日末 $E_T \neq E_{final}$，若通过未计价的期末补电实现终态平衡，属于虚构收益的作弊行为；
  - 统一约束：要求固定基准策略与 MILP 策略在完全相同的初始能量、终态能量、功率与 SOC 物理边界下比较，确保回测公平可信。

---

## 5. 存储架构与 5 级状态信标

- **存储方案**：
  - 核心分析数据：基于 Apache Parquet（按 `date` 与 `series_id` 分区存储，Snappy 压缩）。
  - 审计与追踪：SQLite（`voltpulse.db`）记录每日运行元数据、数据质量校验流水与哈希签名。
- **5 级运行健康状态**：
  - `healthy`：数据采集、清洗、优化全部正常，处于 24 小时预期时效内。
  - `awaiting_publication`：调度未到官方指定发布时间（如每日 17:00 前）。
  - `stale`：超过预期时效且未能拉取到最新批次。
  - `failed`：校验不达标或优化求解器异常。
  - `unavailable`：系统初始化或无有效历史。
- **决策理由**：
  - 坚决杜绝“无数据时假装正常”或“爬虫失败就盲目篡改时间戳”，状态透明可信是向电网与国企展示专业性的核心要素。
