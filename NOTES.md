# VoltPulse 设计手记与工程踩坑记录 (Developer Notes)

> 本文档记录了 VoltPulse 在架构设计、运筹优化求解器选型、电网现货特性对比及工程落地过程中的真实权衡与思考，供交流与技术面试参考。

---

## 1. 为什么放弃 PuLP / Pyomo，直接采用 SciPy HiGHS MILP？

在原型设计初期，曾考虑过使用高校和科研中常用的建模框架（如 `PuLP` 或 `Pyomo`）：
- **依赖与部署痛点**：`PuLP` 默认依赖系统路径中的外部独立求解器二进制（如 CBC 或 GLPK）。在 Windows 和 Linux 跨平台持续集成（CI）及生产环境部署时，经常面临动态链接库（DLL / .so）缺失或可执行程序路径找不到的致命环境问题。
- **性能实测优势**：从 SciPy 1.9 开始，官方底层原生集成了高性能 C++ 求解器 **HiGHS**（`scipy.optimize.milp`）。
  - **内存与吞吐**：免去了 Python 层面构建庞大抽象语法树（AST）和文本 LP 文件转储的开销；
  - **求解时延**：在 96 时段（15分钟出清间隔）、包含 4 组决策变量（充放电功率、二元互斥状态、荷电状态 SOC）的标准日前场景下，HiGHS 单日求解耗时稳定在 **12 ~ 18 ms**，速度比 PuLP + CBC 提升了 4 倍以上，完全支持高频次全生命周期滚动回测。

---

## 2. 山东现货 vs 江苏现货：两类典型电网特性的调度博弈

本项目重点对中国最具代表性的两个现货试点省份进行了实证建模对比：

### 2.1 山东（新能源富集型 / 单深 V 鸭子曲线）
- **电网特征**：全国分布式光伏第一大省（装机超 6000 万千瓦），午间光伏大发造成净负荷严重凹陷，在春季节假日（如“五一”）甚至出现连续数十小时的出清负电价（最低 -80 元/MWh）。
- **调度策略**：典型的**“一充一放”**。在午间低谷或负电价时段（11:00 - 15:00）全力受电充电（甚至获取电网消纳补贴），在晚高峰（18:00 - 22:00）全容量放电。

### 2.2 江苏（重负荷工业枢纽 / 两峰两谷特性）
- **电网特征**：全国第一工业用电大省，夏季/冬季最高负荷突破 1.4 亿千瓦。负荷曲线呈现显著的**“早高峰（09:00-11:30）”**与**“晚高峰（18:30-21:30）”**双峰格局。
- **调度策略**：必须采用**“两充两放（Two Cycles per Day）”**。
  - 第一循环：夜间谷电（01:00-05:00）充电 $\rightarrow$ 早高峰放电；
  - 第二循环：午间平段/光伏小凹陷（12:30-15:00）补充 $\rightarrow$ 晚高峰放电。
- **关键工程权衡（Trade-off）**：
  在江苏场景下，如果只算电能量账面价差，双充双放每日收益显著高于单充单放；但日等效满充满放循环次数（EFC）会从 0.85 骤增至 1.70 以上。若将电池电芯的循环衰减成本（以 30 元/MWh 的电芯全生命周期衰减摊销计入目标函数），实际边际净利润会收窄约 15%~20%。**在做独立储能投资决策时，忽略电芯退化成本将导致收益被严重高估。**

---

## 3. 优化模型中的二元互斥物理硬闭锁 ($u_t$)

在单纯的线性规划（LP）松弛下，若遇到电价恒定或价差无法覆盖充放损耗的时段，数学求解器为了满足终端 SOC 约束，可能会在同一时刻出现微量的充电功率与放电功率同时大于 0（在数学上两者相互抵消，但在物理上是不可能的能量内耗）。

VoltPulse 在 `BESSOptimizer` 中引入了二元指示变量 $u_t \in \{0, 1\}$：
$$c_t \le P_c \cdot u_t$$
$$d_t \le P_d \cdot (1 - u_t)$$
从而在物理硬件层面对充电机（PCS）进行互斥闭锁，确保任何时刻决不可能同时出现充电与放电动作。

---

## 4. 持续集成与跨平台工程踩坑记录

在构建 GitHub Actions 多平台（Ubuntu Linux + Windows）自动化矩阵时，遇到了一个隐蔽但典型的工程问题：
- **现象**：本地 Windows 环境测试 100% 通过，但代码推送到 GitHub Ubuntu-latest 运行时，独立技术审查探针（Probe 1）在比对山东原始 CSV 样本哈希时抛出断言异常。
- **排查**：Windows 默认签出换行符为 CRLF (`\r\n`)，Ubuntu 默认签出为 LF (`\n`)。对于普通的表格处理，Pandas 会自动兼容；但审计探针直接采用 `read_bytes()` 严格校验底层字节 SHA-256，导致了单字节换行差异带来的哈希突变。
- **解决**：在项目根目录引入严格的 `.gitattributes`，将核心审计证据与标杆数据标记为 `-text`（强制二进制安全保存），杜绝跨系统换行符篡改，实现了 Linux 与 Windows 云端矩阵的完全绿灯通行。

---

## 5. 项目后续规划与局限性声明

1. **日前与日内联合优化（Two-stage Stochastic Optimization）**：目前回测以日前现货出清价为主，后续计划增加日前申报 + 日内实时滚动的两阶段随机规划。
2. **容量电价与辅助服务收益联合建模**：针对江苏电力现货市场改革进程中的调频辅助服务（AGC）及容量补偿机制，进一步完善综合收益评估模块。
